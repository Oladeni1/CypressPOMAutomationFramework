#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../browserstack-cypress-cli/bin/runner.js" "$@"
  ret=$?
else 
  node  "$basedir/../browserstack-cypress-cli/bin/runner.js" "$@"
  ret=$?
fi
exit $ret
